#!/bin/sh

if [ "$HOSTNAME" = "silver.davidterei.com" ]; then
  rsync \
    -rtu \
    --progress \
    --exclude=.DS_Store \
    --exclude=iTunes \
    blk:/home/davidt/Music \
    /Users/davidt/
elif [ "$HOSTNAME" = "black.davidterei.com" ]; then
  rsync \
    -rtu \
    --progress \
    --exclude=.DS_Store \
    --exclude=iTunes \
    slv:/Users/davidt/Music \
    /home/davidt/
else
  echo "Can only be run from silver.davidterei.com or black.davidterei.com"
  exit 1
fi

