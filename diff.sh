echo === aliases diff ===
diff -u ~/.aliases .aliases
echo

echo === bashrc diff ===
diff -u ~/.bashrc .bashrc
echo

echo === fonts.conf diff ===
diff -u ~/.fonts.conf .fonts.conf
echo

echo === muttrc diff ===
diff -u ~/.muttrc .muttrc
echo

echo === procmailrc diff ===
diff -u ~/.procmailrc .procmailrc
echo

echo === profile diff ===
diff -u ~/.profile .profile
echo

echo === screenrc diff ===
diff -u ~/.screenrc .screenrc
echo

echo === shenv diff ===
diff -u ~/.shenv .shenv
echo

echo === ssh_startup.sh diff ===
diff -u ~/.ssh_startup.sh .ssh_startup.sh
echo

echo === ssh config diff ===
diff -u ~/.ssh/config config
echo

echo === zshrc diff ===
diff -u ~/.zshrc .zshrc
echo

echo === zshenv diff ===
diff -u ~/.zshenv .zshenv
echo

