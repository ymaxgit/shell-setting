vbell off

#change the hardstatus settings to give an window list at the bottom of the
#screen, with the time and date and with the current window highlighted
hardstatus alwayslastline
hardstatus string '%{= kG}%-Lw%{= kW}%50> %n%f* %t%{= kG}%+Lw%< %{= kG}%-=%D %d %M %Y %c:%s%{-}'

# bigger scrollback buffer
defscrollback 5000

# allow file transferswith sz and rz
zmodem catch

startup_message off

# copy to clipboard command (WONT WORK)
#bind b eval "writebuf" "exec sh -c 'xclip -verbose -in -selection c < /tmp/screen-exchange'"
#bind e eval "stuff ' '" writebuf "exec sh -c 'xsel < /tmp/screen-exchange'"
#bindkey -m > eval "stuff ' '" writebuf "exec sh -c 'xsel -b < /tmp/screen-exchange'"

